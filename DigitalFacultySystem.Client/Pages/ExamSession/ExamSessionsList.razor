@page "/examSessions"
@attribute [Authorize(Roles = "admin, staff")]

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h3>Sezonet e Provimeve</h3>
        <a href="examSession" class="btn btn-primary mb-3">Shto Sezon të Ri</a>
    </div>

    @if (examSessions == null)
    {
        <p><em>Duke ngarkuar...</em></p>
    }
    else
    {
        <div class="table-responsive">
            <table class="table table-striped table-hover">
                <thead class="table-light">
                    <tr>
                        <th scope="col">Emri</th>
                        <th scope="col">Data e Fillimit</th>
                        <th scope="col">Data e Mbarimit</th>
                        <th scope="col" class="text-end pe-3">Veprimet</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var row in examSessions)
                    {
                        <tr>
                            <td>@row.Name</td>
                            <td>@row.StartDate</td>
                            <td>@row.EndDate</td>
                            <td class="text-end pe-3"><a href="@("examSession/" + row.Id)" class="btn btn-warning">Edito</a></td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
</div>
